# Frontend environment configuration

# API Base URL - overrides the default in App.tsx
# Default behavior:
#   - Development (npm run dev): http://localhost:8000
#   - Production (Docker): http://host.docker.internal:8000
# VITE_API_URL=http://localhost:8000

# Node environment (set by docker-compose.yml)
NODE_ENV=production
